*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties
# The Kotlin compiler's own scratch directory — session files and error logs
# from failed builds. It sits beside the modules rather than inside build/,
# which is why the usual rule above misses it, and it is how a stray
# errors-*.log ends up in a commit.
.kotlin/
