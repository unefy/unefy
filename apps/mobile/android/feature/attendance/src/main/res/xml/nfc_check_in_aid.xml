<?xml version="1.0" encoding="utf-8"?>
<!-- The AID Android routes to MemberCardService. Must match CheckInApdu.AID
     byte for byte; the two are separated only because one is XML. -->
<host-apdu-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/nfc_service_description"
    android:requireDeviceUnlock="true">
    <aid-group
        android:description="@string/nfc_aid_group_description"
        android:category="other">
        <aid-filter android:name="F0756E656679" />
    </aid-group>
</host-apdu-service>
