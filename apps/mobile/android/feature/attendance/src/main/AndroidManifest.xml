<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The supervisor's scanner. Declared by the feature that needs it rather
         than in the app manifest, so the permission travels with the code that
         justifies it. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- The second transport. Not required: the QR path stays the universal
         one, for iPhones and for phones without an NFC chip. -->
    <uses-permission android:name="android.permission.NFC" />

    <!-- The card service buzzes on a tap, and it has no screen to do it any
         other way. Install-time, so nothing to ask the user for. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="false" />

    <application>
        <service
            android:name=".nfc.MemberCardService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/nfc_check_in_aid" />
        </service>
    </application>

</manifest>
