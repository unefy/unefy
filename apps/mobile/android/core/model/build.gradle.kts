// Pure Kotlin — no Android dependencies, by design. See the module dependency
// rules in apps/mobile/CLAUDE.md.
plugins {
    id("unefy.jvm.library")
}

dependencies {
    testImplementation(libs.junit)
}
